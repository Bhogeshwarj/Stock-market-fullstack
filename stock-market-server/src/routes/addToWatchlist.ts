import express from 'express';
// import { addStockToWatchlist } from '../controller/watchlistController';

const router = express.Router();

// router.post('/add', addStockToWatchlist);

export default router;
